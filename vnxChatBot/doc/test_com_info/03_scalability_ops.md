⚡ Quản lý Tài nguyên, Hiệu năng & Mở rộng Hệ thống (Scalability & Ops)1. Kiểm soát Tài nguyên & Chi phí (Cost & Resource Governance) 💳Hạn mức Quota: Tích hợp kiểm tra giới hạn gói cước thông qua phân hệ apps.subscriptions trước khi thực thi các tác vụ tiêu tốn tài nguyên LLM hoặc bóc tách tệp lớn.Semantic Intent Caching: Lưu trữ và truy vấn kết quả trên Redis RAM với ngưỡng tương đồng $\ge 0.92$ giúp trả kết quả dưới 2 mili-giây, cắt giảm tối đa chi phí tiêu thụ Token trùng lặp.2. Giải pháp Chống nghẽn Cổ chai & Mở rộng (Scalability Architecture) 🚀Xử lý Bất đồng bộ: Tuyệt đối không xử lý file nặng qua HTTP Request. Sử dụng Celery + Redis làm Task Queue cho các tác vụ Parsing, Chunking và Embedding.Vector Search Optimization: Bắt buộc áp dụng Metadata Filtering (metadata={"group_id": current_group_id}) trong mọi truy vấn Vector Store để cô lập dữ liệu.Quản lý Kết nối Realtime: Sử dụng Django Channels (ASGI) kết hợp Redis Channel Layer để vận hành hàng ngàn kết nối WebSocket song song không block server.3. Kiến trúc Xử lý Mâu thuẫn Hiệu suất Cao 🔄Vectorized Pre-filtering: Dùng Cosine Similarity trên phân vùng group_id lọc nhanh top 3 đoạn văn bản tương đồng cao nhất thay vì để LLM đọc toàn bộ kho.Asynchronous Conflict Workers: Tác vụ phân tích mâu thuẫn được đẩy vào Celery queue riêng biệt để không ảnh hưởng đến luồng chat chính.Optimistic Locking: Sử dụng cơ chế khóa lạc quan trên bảng KnowledgeChapter để tránh xung đột ghi đồng thời (race conditions).4. Phân rã Luồng Thực thi & Ưu tiên Tài nguyên (Execution Pipeline) 📊Mức độ ưu tiên 🏷️Luồng Thực thi ⚙️Mô tả & Mục đích Vận hành 💡P0 (Critical)Chat Realtime & Tư vấn AIƯu tiên cao nhất, đọc trực tiếp Semantic Cache hoặc Vector Store cô lập theo group_id.P1 (Background)Xử lý file & Trích xuấtKích hoạt qua Celery Task (process_document_task) khi người dùng tải lên tài liệu mới.P2 (Low)Tổng hợp Tri thức NhómĐịnh kỳ phân tích các thảo luận để cập nhật tri thức nền tảng (Group Learning Loop).P3 (Maintenance)Quản trị Kiến trúcLưu trữ sơ đồ kỹ thuật và nội soi hệ thống (SystemBlueprint).