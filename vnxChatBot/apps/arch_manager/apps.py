from django.apps import AppConfig

class ArchManagerConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.arch_manager'
    verbose_name = 'Quản lý sơ đồ cấu trúc hệ thống Dev'