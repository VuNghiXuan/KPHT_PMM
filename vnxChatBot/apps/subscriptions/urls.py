# apps/subscriptions/urls.py
from django.urls import path
from . import views # Giả định bạn có file views.py trong app này

app_name = 'subscriptions'

urlpatterns = [
    # Thêm các đường dẫn tại đây, ví dụ:
    # path('', views.subscription_list, name='list'),
]