class BusinessAgent:
    """Agent nhận diện nghiệp vụ và gợi ý người dùng nhập liệu"""
    
    def check_form_completion(self, region_id):
        # Kiểm tra xem các ô bắt buộc trong vùng (TAB) đã nhập đủ chưa
        # Nếu thiếu, trả về câu thoại hướng dẫn cho Chatbot
        pass

    def detect_conflicts(self, field_id):
        # So sánh công thức ô này với các ô cùng nhãn trong 300 sheet khác
        pass